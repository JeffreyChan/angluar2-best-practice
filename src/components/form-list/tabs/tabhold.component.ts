import { Component, ContentChildren, QueryList, AfterContentInit, forwardRef } from '@angular/core';
import { TabComponent } from './tab.component';

@Component({
    selector: 'tab-hold',
    templateUrl: 'tabhold.component.html',
})
export class TabHoldComponent {
    constructor() {

    }
}